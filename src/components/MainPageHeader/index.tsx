import { Header, HeaderTitle } from './index.styles';

const MainPageHeader = () => {
  return (
    <Header>
      <HeaderTitle>notion clone</HeaderTitle>
    </Header>
  );
};

export default MainPageHeader;
